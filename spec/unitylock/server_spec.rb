require 'spec_helper'

describe Unitylock::Server do
  it 'has a version number' do
    expect(Unitylock::Server::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
